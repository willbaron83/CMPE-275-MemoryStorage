syntax = "proto3";

service FileServer {
  rpc upload(stream Chunk) returns (Reply) {}
}

message Chunk {
  bytes buffer = 1;
}

message Request {
  string name = 1;
}

message Reply {
  bool success = 1;
}
